# Aurora Hotel Django

Учебный Django-проект сайта отеля.

## Запуск

```powershell
py -m pip install -r requirements.txt
py manage.py migrate
py manage.py runserver
```

Откройте `http://127.0.0.1:8000/`.

Страницы:

- `/` — главная
- `/rooms/` — номера
- `/contact/` — контакты и бронирование
